server:
  port: 3377

spring:
  application:
    name: cloud-config-alibaba
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
        #group: TEST_GROUP
        namespace: dev

#${spring.application.name}-${spring.profile.active}.${spring.cloud.nacos.config.file-extension}
#cloud-config-alibaba-dev.yaml



